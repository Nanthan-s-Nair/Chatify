import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Chatify',
  description: 'Transform your Spotify experience with an intelligent chatbot that understands your mood, plays your favorite tracks, and engages in meaningful conversations.',
  icons: {
    icon: '/logo.jpg',
  },
};


export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
